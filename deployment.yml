apiVersion: apps/v1
kind: Deployment
metadata:
  name: proj2_cluster
spec:
  replicas: 2
  selector:
    matchLabels:
      app: worker_k8s
  template:
    metadata:
      labels:
        app: worker_k8s
    spec:
      containers:
      - name: con_prod
        image: k8s_cluster
        ports:
        - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: proj2_service
spec:
  type: NodePort
  selector:
    app: worker_k8s
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30008
